# College Event Feedback Analysis

Python project for analyzing student feedback from college events.

Run the analysis with:

```bash
python -m pip install -r requirements.txt
python analyze_feedback.py --input data/student_feedback.csv
```
